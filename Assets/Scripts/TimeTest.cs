using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimeTest : MonoBehaviour
{
    /*
    private float Delay = 1;

    private float LastTime = 20;

    private void Update()
    {
        if(Time.time - LastTime >= Delay)
        {
            TestTime();
        }
    }

    private void TestTime()
    {
        LastTime = Time.time;
        Debug.Log(Time.time);
    }
    */
}
